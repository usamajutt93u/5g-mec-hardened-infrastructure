apiVersion: v1
kind: Service
metadata:
  name: cnf-gateway-svc
  namespace: default
spec:
  selector:
    app: cnf-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
